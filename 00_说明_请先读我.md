# 论文复现成果包 · 说明

> **复现对象**：Liang, J., & Jia, G. (2022). *China futures price forecasting based on online search and information transfer.* Data Science and Management, 5, 187–198.
> **作者**：Jingyi Liang、Guozhu Jia（四川师范大学物理与电子工程学院）
> **复现完成日期**：2026-08-15

---

## 一、这个文件夹里有什么

| 文件夹 | 内容 | 建议怎么看 |
|---|---|---|
| `01_报告与图表/` | 复现报告（PDF / Word）+ 玉米对比图（PNG） | **先看这个**：打开 `复现报告.pdf`，再看对比图 |
| `02_代码/` | 8 个 Python 脚本（完整复现管线） | 看 `代码说明.md` 了解执行顺序 |
| `03_数据/` | 期货价格 + 百度指数 + 谷歌趋势 + 中间产物 | 看 `数据说明.md` 了解每类数据 |
| `04_论文与笔记/` | 精读笔记与对比表（原文 PDF 不含，DOI 见该目录 README） | 参考 |

## 二、复现做了什么（三步管线）

```
百度指数(中文9词) + 谷歌趋势(英文4词)
        │ 对数 → 时滞平移 → z-score → PCA(特征值>1) → 式(4)加权
        ▼
      ICPI（网络消费者物价指数）
        │ 传递熵 TE（KSG 估计，窗口60/步长1）
        ▼
  [开盘价] vs [开盘价+ICPI+TE] → GWO-CNN-LSTM 预测
```

## 三、核心结论（诚实汇报）

1. **流程忠实复现成功**：关键词映射、预处理顺序、PCA、传递熵、GWO-CNN-LSTM 均与论文对齐。
2. **但融合模型的增益未能复现**：5 个品种中 4 个「价格+ICPI+TE」反而比「仅价格」更差（玉米 -31.6%、大豆 -157%、PVC -2.6%、螺纹钢 -13%），仅鸡蛋 +7.3% 小幅改善。
3. **根因已定位**：我们构建的 ICPI 只有 3 个主成分 / 68.33% 方差（论文 4 个 / 79.01%）。进一步溯源是谷歌趋势 4 个英文词太同质、塌缩成 1 个因子，少了第 4 个主成分；本质是 Google Trends「相对热度」数据会回溯修订，论文 2021 年抓的与我们 2026 年抓的同一批词数值对不上，属数据层难以完全还原的差异。

> 详细过程、全部数字和排查证据见 `01_报告与图表/复现报告.pdf`。

## 四、如何重跑代码（可选）

1. 环境：Python 3 + PyTorch + pandas + scikit-learn + pytrends
2. 需先开科学上网（抓谷歌趋势用）
3. 脚本里的路径按 `data/` 书写：把 `02_代码` 里的 8 个脚本复制到 `03_数据` 的**上一级**，并把 `03_数据` 重命名为 `data`，即可按 `代码说明.md` 里的顺序运行
4. Windows 上跑 torch 脚本前，先 `set KMP_DUPLICATE_LIB_OK=TRUE`
